@layout('layouts/main')
@set('title', 'MovieDB')

@section('body')
<div>
  <ul>

    @each (movie in movies)
    <a href="/movie/{{movie.id}}">
      <li>
        <p>{{movie.title}}</p>
        <p>{{movie.description}}</p>
        <p>{{movie.release}}</p>
      </li>
    </a>
    @end

  </ul>

</div>

@end